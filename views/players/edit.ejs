<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Plyaer edit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body class="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Club&Players</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/clubs">Clubs</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/players">Players</a>
            </li>
            
          </ul>
        </div>
      </div>
    </nav>

    <h3>Update player with id #<%= player._id %></h3>

    <form class="mt-5" action="/players/<%= player._id %>" method="post">
      <div class="mb-3">
        <label for="first_name" class="form-label">First Name</label>
        <input type="text" name="first_name" class="form-control" value="<%= player.first_name %>">
      </div>

      <div class="mb-3">
        <label for="last_name" class="form-label">Last Name</label>
        <input type="text" name="last_name" class="form-control" value="<%= player.last_name %>">
      </div>

      <div class="mb-3">
        <label for="address" class="form-label">Position</label>
        <input type="text" name="position" class="form-control" value="<%= player.position %>">
      </div>

      <div class="form-group mb-3">
        <label for="club">Club</label>
        <select class="form-control" id="club" name="club">
          <% if (player.club) { %>
            <% clubs.forEach(club => { %>
              <% if (club._id.equals(player.club._id)) { %>
                <option selected value="<%= club._id %>"><%= club.name %></option>
              <% } else { %>
                <option value="<%= club._id %>"><%= club.name %></option>
              <% } %>
            <% }) %>
          <% } else { %>
            <option value="">Choose a club</option>
            <% clubs.forEach(club => { %>
              <option value="<%= club._id %>">
                <%= club.name %>
              </option>
            <% }) %>
          <% } %>
        </select>
      </div>

      <button class="btn btn-success" type="submit">Update</button>
    </form>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
